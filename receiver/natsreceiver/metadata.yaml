type: nats

status:
  class: receiver
  stability:
    development: [metrics, logs, traces]
  distributions: []
  codeowners:
    # TODO: assemble >=3 cross-company code owners (incl. a sponsor who is an
    # approver/maintainer) before opening the donation PR. See tracking issue #39540.
    active: []

tests:
  skip_lifecycle: true
